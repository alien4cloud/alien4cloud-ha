server {
        resolver 127.0.0.1 valid=10s;
        listen %LISTEN_PORT%;
        server_name %SERVER_NAME%;
        location / {
                proxy_pass      http://%SERVICE_FQN%:%SERVICE_PORT%/;
        }
}
